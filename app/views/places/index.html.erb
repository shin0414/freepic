<%= render "header" %>
<div class="introduction">
  <h1>〜 欲しい画像を欲しい時に 〜</h1>
  <p>このサイトは、著作権フリーの画像を募集したり、共有したりできるサイトです。</p>
</div>
<div class= "sub_header">
  <div class= "sub_left">
    <p>募集一覧</p>

    <% if user_signed_in? %>
      <%= link_to new_place_path,class:"newplace" do %>
        <i class="fas fa-plus"></i>
      <% end %>
    <% else %>
      <%= link_to new_user_session_path, class: "newplace" do %>
        <i class="fas fa-plus"></i>
      <% end %>
    <% end %>

    
  </div>
  <p class= "sub_right">pick up pictures</P>
</div>
<div class= "placebox">
  <div class= "leftbox">
    <ul>
      <% @places.each do |place| %>
        <%= link_to place_path(place.id) do %>
          <li>
            <p><%= place.title %></p>
            <p><%= place.updated_at %></p>
          </li>
        <% end %>
      <% end %>
    </ul>
  </div>
  <div class= "rightbox">
    <% @pictures.each do |p| %>
      <%= link_to place_picture_path(p.place_id, p.id) do %>
        <%= image_tag p.src.url %>
      <% end %>
    <% end %>
          

    
    
  </div>
</div>
<%= render "footer" %>
