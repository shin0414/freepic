<div class="placebox_show">
  <div class="thread_title">
    <div>
      <p><%= @place.title %></P>
      <p><%= @place.updated_at %></p>
      <p><%= @place.explanation %></p>
      
    </div>
    <div>
      <%= link_to new_place_picture_path(@place),class:"newpicture" do %>
        <i class="fas fa-camera"></i>
      <% end %>
      <%= link_to new_place_post_path(@place),class:"newpost" do %>
        <i class="fas fa-pen pen_small"></i>
      <% end %>
      <%= link_to place_path(@place), method: :delete, local: true do %>
        <i class="fas fa-trash-alt"></i>
      <% end %>
    </div>
  </div>
  <div class="thread">
    <% @place.posts.each do |p| %>
      <div class="posts">
        <div class="post">
          <div class="post__child">
            <p class="timestamp"><%= p.updated_at %></p>
            <%= p.text %>
          </div>
        </div>
      </div>
    <% end %>
    <% @place.pictures.each do |p| %>
      <div>
        <div class="picture_box">
          <p class="timestamp"><%= p.updated_at %></p>
          <%= link_to place_picture_path(@place.id, p.id) do %>
            <%= image_tag p.src.url %>
          <% end %>
          
        </div>
      </div>
    <% end %>

    

    <%# <% test = [] %>
    <%#<% @place.posts.each do |post| %>
      <%#<% test << post %>
    <%#<% end %>
    <%#<% @place.pictures.each do |picture| %>
      <%#<% test << picture %>
    <%#<% end %>



    <%#<% test.each do |p| %>
      <%#<div class="posts">
        <div class="post">
          <div class="post__child">
            <p class="timestamp"><%= p.updated_at %>
            <%#</p>
            <%#<%= p.text %>
          <%#</div>
        </div>
      </div>
    <% end %> 

    

    



    
  </div>
  <%= render "footer" %>
</div>